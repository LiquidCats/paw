name: Go CI

on:
  pull_request:
  push:

concurrency:
  group: go-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  changes:
    name: Detect changed modules
    runs-on: ubuntu-latest
    outputs:
      modules: ${{ steps.filter.outputs.changes }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed modules
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            lib/evm-input-parser:
              - 'lib/evm-input-parser/**'
            lib/graceful:
              - 'lib/graceful/**'
            lib/jsonrpc:
              - 'lib/jsonrpc/**'
            lib/observer:
              - 'lib/observer/**'
            lib/workers:
              - 'lib/workers/**'
            services/kms:
              - 'services/kms/**'
            services/rater:
              - 'services/rater/**'
            services/watcher:
              - 'services/watcher/**'

  test:
    name: Test ${{ matrix.module }}
    needs: changes
    if: ${{ needs.changes.outputs.modules != '[]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        module: ${{ fromJSON(needs.changes.outputs.modules) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.4"
          cache-dependency-path: |
            **/go.mod
            **/go.sum
            go.work.sum

      - name: Prepare Go workspace
        run: |
          go work use \
            ./lib/evm-input-parser \
            ./lib/graceful \
            ./lib/jsonrpc \
            ./lib/observer \
            ./lib/workers \
            ./services/kms \
            ./services/rater \
            ./services/watcher

      - name: Download dependencies
        working-directory: ${{ matrix.module }}
        run: go mod download

      - name: Run tests
        working-directory: ${{ matrix.module }}
        run: go test -count=1 ./...

  lint:
    name: Lint ${{ matrix.module }}
    needs: changes
    if: ${{ needs.changes.outputs.modules != '[]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        module: ${{ fromJSON(needs.changes.outputs.modules) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select golangci-lint version
        id: golangci
        run: |
          case "${{ matrix.module }}" in
            lib/workers)
              version="v2.5.0"
              ;;
            services/rater)
              version="v2.10.1"
              ;;
            services/watcher)
              version="v2.11.1"
              ;;
            *)
              version="v2.11.4"
              ;;
          esac
          echo "version=${version}" >> "${GITHUB_OUTPUT}"

      - name: Run golangci-lint
        run: |
          docker run --rm \
            -e GOWORK=off \
            -v "${GITHUB_WORKSPACE}:/src" \
            -w "/src/${{ matrix.module }}" \
            "golangci/golangci-lint:${{ steps.golangci.outputs.version }}-alpine" \
            golangci-lint run ./...
